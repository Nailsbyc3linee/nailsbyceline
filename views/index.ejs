<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= business.name %> - Professional Nail Services</title>
    <meta
      name="description"
      content="<%= business.name %> - <%= business.tagline %>. Professional nail services including manicures, pedicures, nail art, and extensions."
    />

    <!-- Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />

    <style>
      .hero-gradient {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      }

      .nail-gradient {
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
      }

      .elegant-gradient {
        background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
      }

      .floating {
        animation: floating 3s ease-in-out infinite;
      }

      @keyframes floating {
        0%,
        100% {
          transform: translateY(0px);
        }
        50% {
          transform: translateY(-10px);
        }
      }

      .fade-in {
        animation: fadeIn 1s ease-in;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .image-placeholder {
        background: linear-gradient(45deg, #f3f4f6, #e5e7eb);
        background-size: 20px 20px;
        background-image: radial-gradient(circle, #d1d5db 1px, transparent 1px);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #9ca3af;
        font-size: 14px;
        text-align: center;
        border: 2px dashed #d1d5db;
        transition: all 0.3s ease;
      }

      .image-placeholder:hover {
        border-color: #f093fb;
        background: linear-gradient(45deg, #fef7ff, #fce7f3);
      }

      .service-card {
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .service-card:hover {
        transform: translateY(-8px) scale(1.02);
      }

      .glass-effect {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
      }
    </style>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="fixed top-0 w-full bg-white/90 backdrop-blur-lg shadow-lg z-50">
      <div class="container mx-auto px-4 py-3">
        <div class="flex justify-between items-center">
          <div class="flex items-center space-x-2">
            <i class="fas fa-gem text-2xl text-pink-500"></i>
            <span
              class="text-xl font-bold text-gray-800"
              style="font-family: 'Playfair Display', serif"
              ><%= business.name %></span
            >
          </div>
          <div class="hidden md:flex space-x-6">
            <a
              href="#home"
              class="text-gray-700 hover:text-pink-500 transition-colors"
              >Home</a
            >
            <a
              href="#gallery"
              class="text-gray-700 hover:text-pink-500 transition-colors"
              >Gallery</a
            >
            <a
              href="#services"
              class="text-gray-700 hover:text-pink-500 transition-colors"
              >Services</a
            >
            <a
              href="#contact"
              class="text-gray-700 hover:text-pink-500 transition-colors"
              >Contact</a
            >
          </div>
          <a
            href="tel:<%= business.phone %>"
            class="bg-pink-500 text-white px-4 py-2 rounded-full hover:bg-pink-600 transition-colors"
          >
            <i class="fas fa-phone mr-2"></i>Book Now
          </a>
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <section
      id="home"
      class="min-h-screen hero-gradient flex items-center justify-center relative overflow-hidden"
    >
      <div class="absolute inset-0 opacity-20">
        <div
          class="absolute top-10 left-10 w-20 h-20 bg-white rounded-full floating"
        ></div>
        <div
          class="absolute top-1/3 right-20 w-16 h-16 bg-pink-200 rounded-full floating"
          style="animation-delay: -1s"
        ></div>
        <div
          class="absolute bottom-1/4 left-1/4 w-12 h-12 bg-purple-200 rounded-full floating"
          style="animation-delay: -2s"
        ></div>
      </div>

      <div class="container mx-auto px-4 text-center relative z-10">
        <div class="max-w-4xl mx-auto fade-in">
          <h1
            class="text-5xl md:text-7xl font-bold text-white mb-6"
            style="font-family: 'Playfair Display', serif"
          >
            <%= business.name %>
          </h1>
          <p class="text-xl md:text-2xl text-white/90 mb-8">
            <%= business.tagline %>
          </p>
          <div
            class="flex flex-col md:flex-row gap-4 justify-center items-center mb-12"
          >
            <a
              href="#services"
              class="bg-white text-purple-700 px-8 py-4 rounded-full font-semibold hover:bg-gray-100 transition-all transform hover:scale-105"
            >
              View Services
            </a>
            <a
              href="tel:<%= business.phone %>"
              class="border-2 border-white text-white px-8 py-4 rounded-full font-semibold hover:bg-white hover:text-purple-700 transition-all transform hover:scale-105"
            >
              <i class="fas fa-phone mr-2"></i><%= business.phone %>
            </a>
          </div>

          <!-- Hero Gallery Placeholders -->
          <div class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-4xl mx-auto">
            <div class="image-placeholder h-32 rounded-lg glass-effect">
              <div class="text-center">
                <i class="fas fa-image text-2xl mb-2"></i>
                <p class="text-xs">Hero Image 1<br />Nail Art Showcase</p>
              </div>
            </div>
            <div class="image-placeholder h-32 rounded-lg glass-effect">
              <div class="text-center">
                <i class="fas fa-image text-2xl mb-2"></i>
                <p class="text-xs">Hero Image 2<br />Manicure Process</p>
              </div>
            </div>
            <div class="image-placeholder h-32 rounded-lg glass-effect">
              <div class="text-center">
                <i class="fas fa-image text-2xl mb-2"></i>
                <p class="text-xs">Hero Image 3<br />Studio Interior</p>
              </div>
            </div>
            <div class="image-placeholder h-32 rounded-lg glass-effect">
              <div class="text-center">
                <i class="fas fa-image text-2xl mb-2"></i>
                <p class="text-xs">Hero Image 4<br />Happy Client</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Gallery Section -->
    <section id="gallery" class="py-20 bg-white">
      <div class="container mx-auto px-4">
        <div class="text-center mb-16">
          <h2
            class="text-4xl md:text-5xl font-bold text-gray-800 mb-4"
            style="font-family: 'Playfair Display', serif"
          >
            My Beautiful Work
          </h2>
          <p class="text-xl text-gray-600 max-w-2xl mx-auto">
            Discover my stunning nail art creations and see why my clients
            love my work
          </p>
        </div>

        <!-- Featured Gallery Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
          <div
            class="image-placeholder h-80 rounded-xl hover:scale-105 transition-transform"
          >
            <div class="text-center">
              <i class="fas fa-image text-4xl mb-4 text-pink-400"></i>
              <h3 class="text-lg font-semibold mb-2">Featured Work 1</h3>
              <p class="text-sm">
                Elegant French Manicure<br />with Gold Accents
              </p>
            </div>
          </div>
          <div
            class="image-placeholder h-80 rounded-xl hover:scale-105 transition-transform"
          >
            <div class="text-center">
              <i class="fas fa-image text-4xl mb-4 text-pink-400"></i>
              <h3 class="text-lg font-semibold mb-2">Featured Work 2</h3>
              <p class="text-sm">Colorful Nail Art<br />Geometric Patterns</p>
            </div>
          </div>
          <div
            class="image-placeholder h-80 rounded-xl hover:scale-105 transition-transform"
          >
            <div class="text-center">
              <i class="fas fa-image text-4xl mb-4 text-pink-400"></i>
              <h3 class="text-lg font-semibold mb-2">Featured Work 3</h3>
              <p class="text-sm">Acrylic Extensions<br />with Ombre Design</p>
            </div>
          </div>
        </div>

        <!-- Gallery Grid -->
        <div
          class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-4"
        >
          <% for(let i = 1; i <= 15; i++) { %>
          <div
            class="image-placeholder h-48 rounded-lg hover:scale-105 transition-transform"
          >
            <div class="text-center">
              <i class="fas fa-image text-2xl mb-2 text-gray-400"></i>
              <p class="text-xs">Gallery <%= i %></p>
            </div>
          </div>
          <% } %>
        </div>

        <!-- Instagram Feed Placeholder -->
        <div class="mt-16 text-center">
          <h3 class="text-2xl font-bold text-gray-800 mb-8">
            Follow Me on Instagram
          </h3>
          <div class="grid grid-cols-3 md:grid-cols-6 gap-4 max-w-4xl mx-auto">
            <% for(let i = 1; i <= 12; i++) { %>
            <div
              class="image-placeholder h-32 rounded-lg hover:scale-105 transition-transform"
            >
              <div class="text-center">
                <i class="fab fa-instagram text-xl text-pink-500"></i>
                <p class="text-xs mt-1">Post <%= i %></p>
              </div>
            </div>
            <% } %>
          </div>
          <a
            href="https://instagram.com/<%= business.socialMedia.instagram.replace('@', '') %>"
            class="inline-block mt-8 bg-gradient-to-r from-pink-500 to-purple-600 text-white px-8 py-3 rounded-full font-semibold hover:from-pink-600 hover:to-purple-700 transition-all transform hover:scale-105"
          >
            <i class="fab fa-instagram mr-2"></i>Follow <%=
            business.socialMedia.instagram %>
          </a>
        </div>
      </div>
    </section>

    <!-- Services Section -->
    <section id="services" class="py-20 elegant-gradient">
      <div class="container mx-auto px-4">
        <div class="text-center mb-16">
          <h2
            class="text-4xl md:text-5xl font-bold text-gray-800 mb-4"
            style="font-family: 'Playfair Display', serif"
          >
            My Services
          </h2>
          <p class="text-xl text-gray-600 max-w-2xl mx-auto">
            Professional nail care services tailored to your style and
            preferences
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          <% business.services.forEach((service, index) => { %>
          <div
            class="service-card bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl"
          >
            <div
              class="image-placeholder h-48 rounded-xl mb-6 hover:scale-105 transition-transform"
            >
              <div class="text-center">
                <i class="fas fa-hand-sparkles text-3xl mb-3 text-pink-500"></i>
                <p class="text-sm font-medium">
                  <%= service.name %><br />Service Image
                </p>
              </div>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-3">
              <%= service.name %>
            </h3>
            <p class="text-gray-600 mb-4"><%= service.description %></p>
            <div class="flex justify-between items-center">
              <span class="text-2xl font-bold text-pink-600"
                ><%= service.price %></span
              >
              <a
                href="tel:<%= business.phone %>"
                class="bg-pink-500 text-white px-6 py-2 rounded-full hover:bg-pink-600 transition-colors"
              >
                Book Now
              </a>
            </div>
          </div>
          <% }) %>
        </div>

        <!-- Before/After Section -->
        <div class="mt-20">
          <h3 class="text-3xl font-bold text-center text-gray-800 mb-12">
            Before & After Transformations
          </h3>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% for(let i = 1; i <= 6; i++) { %>
            <div class="bg-white rounded-2xl p-6 shadow-lg">
              <div class="grid grid-cols-2 gap-4">
                <div class="text-center">
                  <h4 class="text-sm font-semibold text-gray-600 mb-2">
                    BEFORE
                  </h4>
                  <div class="image-placeholder h-40 rounded-lg">
                    <div class="text-center">
                      <i class="fas fa-image text-xl mb-2"></i>
                      <p class="text-xs">Before <%= i %></p>
                    </div>
                  </div>
                </div>
                <div class="text-center">
                  <h4 class="text-sm font-semibold text-gray-600 mb-2">
                    AFTER
                  </h4>
                  <div class="image-placeholder h-40 rounded-lg">
                    <div class="text-center">
                      <i class="fas fa-image text-xl mb-2"></i>
                      <p class="text-xs">After <%= i %></p>
                    </div>
                  </div>
                </div>
              </div>
              <p class="text-center text-sm text-gray-600 mt-3">
                Transformation <%= i %>
              </p>
            </div>
            <% } %>
          </div>
        </div>
      </div>
    </section>

    <!-- Testimonials Section -->
    <section class="py-20 bg-white">
      <div class="container mx-auto px-4">
        <div class="text-center mb-16">
          <h2
            class="text-4xl font-bold text-gray-800 mb-4"
            style="font-family: 'Playfair Display', serif"
          >
            What My Clients Say
          </h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <% for(let i = 1; i <= 3; i++) { %>
          <div
            class="bg-gray-50 rounded-2xl p-8 hover:shadow-lg transition-shadow"
          >
            <div class="flex items-center mb-4">
              <div class="image-placeholder w-16 h-16 rounded-full mr-4">
                <i class="fas fa-user text-gray-400"></i>
              </div>
              <div>
                <h4 class="font-semibold text-gray-800">Client <%= i %></h4>
                <div class="flex text-yellow-400">
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star"></i>
                  <i class="fas fa-star"></i>
                </div>
              </div>
            </div>
            <p class="text-gray-600 italic">
              "Amazing service and beautiful results! I always leave feeling
              pampered and confident. Highly recommend!"
            </p>
          </div>
          <% } %>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="py-20 nail-gradient">
      <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto">
          <div class="text-center mb-16">
            <h2
              class="text-4xl md:text-5xl font-bold text-white mb-4"
              style="font-family: 'Playfair Display', serif"
            >
              Get In Touch
            </h2>
            <p class="text-xl text-white/90 max-w-2xl mx-auto">
              Ready to pamper yourself? Contact me today to book your
              appointment
            </p>
          </div>

          <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
            <!-- Contact Info -->
            <div class="space-y-8">
              <div class="bg-white/10 backdrop-blur-lg rounded-2xl p-8">
                <h3 class="text-2xl font-bold text-white mb-6">
                  Contact Information
                </h3>

                <div class="space-y-4">
                  <div class="flex items-center text-white">
                    <i class="fas fa-phone text-xl mr-4"></i>
                    <div>
                      <p class="font-semibold">Phone</p>
                      <a
                        href="tel:<%= business.phone %>"
                        class="hover:text-pink-200 transition-colors"
                        ><%= business.phone %></a
                      >
                    </div>
                  </div>

                  <div class="flex items-center text-white">
                    <i class="fas fa-envelope text-xl mr-4"></i>
                    <div>
                      <p class="font-semibold">Email</p>
                      <a
                        href="mailto:<%= business.email %>"
                        class="hover:text-pink-200 transition-colors"
                        ><%= business.email %></a
                      >
                    </div>
                  </div>

                  <div class="flex items-start text-white">
                    <i class="fas fa-clock text-xl mr-4 mt-1"></i>
                    <div>
                      <p class="font-semibold">Hours</p>
                      <p><%= business.hours.weekdays %></p>
                      <p><%= business.hours.weekend %></p>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Social Media -->
              <div class="bg-white/10 backdrop-blur-lg rounded-2xl p-8">
                <h3 class="text-2xl font-bold text-white mb-6">Follow Me</h3>
                <div class="flex space-x-4">
                  <a
                    href="https://instagram.com/<%= business.socialMedia.instagram.replace('@', '') %>"
                    class="bg-white/20 hover:bg-white/30 p-3 rounded-full text-white transition-colors"
                  >
                    <i class="fab fa-instagram text-xl"></i>
                  </a>
                  <a
                    href="https://tiktok.com/<%= business.socialMedia.tiktok.replace('@', '') %>"
                    class="bg-white/20 hover:bg-white/30 p-3 rounded-full text-white transition-colors"
                  >
                    <i class="fab fa-tiktok text-xl"></i>
                  </a>
                </div>

                <!-- Social Media Preview -->
                <div class="mt-6 grid grid-cols-3 gap-2">
                  <% for(let i = 1; i <= 6; i++) { %>
                  <div class="image-placeholder h-16 rounded-lg">
                    <i class="fab fa-instagram text-sm text-pink-300"></i>
                  </div>
                  <% } %>
                </div>
              </div>
            </div>

            <!-- Booking Form -->
            <div class="bg-white rounded-2xl p-8 shadow-2xl">
              <h3 class="text-2xl font-bold text-gray-800 mb-6">
                Book Your Appointment
              </h3>
              <form class="space-y-4">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <input
                    type="text"
                    placeholder="First Name"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                  />
                  <input
                    type="text"
                    placeholder="Last Name"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                  />
                </div>
                <input
                  type="email"
                  placeholder="Email Address"
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                />
                <input
                  type="tel"
                  placeholder="Phone Number"
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                />
                <select
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                >
                  <option value="">Select a Service</option>
                  <% business.services.forEach(service => { %>
                  <option value="<%= service.name %>">
                    <%= service.name %> - <%= service.price %>
                  </option>
                  <% }) %>
                </select>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <input
                    type="date"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                  />
                  <input
                    type="time"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                  />
                </div>
                <textarea
                  placeholder="Special requests or notes"
                  rows="4"
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-pink-500 focus:border-transparent"
                ></textarea>
                <button
                  type="submit"
                  class="w-full bg-gradient-to-r from-pink-500 to-purple-600 text-white py-3 rounded-lg font-semibold hover:from-pink-600 hover:to-purple-700 transition-all transform hover:scale-105"
                >
                  Book Appointment
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-16">
      <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
          <!-- Business Info -->
          <div>
            <div class="flex items-center space-x-2 mb-6">
              <i class="fas fa-gem text-2xl text-pink-500"></i>
              <span
                class="text-xl font-bold"
                style="font-family: 'Playfair Display', serif"
              >
                <%= business.name %>
              </span>
            </div>
            <p class="text-gray-300 mb-4"><%= business.tagline %></p>
            <p class="text-gray-400 text-sm leading-relaxed">
              Professional nail care services in a luxurious and relaxing
              environment. Your beauty and satisfaction is my top priority.
            </p>
          </div>

          <!-- Quick Links -->
          <div>
            <h3 class="text-lg font-semibold mb-6">Quick Links</h3>
            <ul class="space-y-3">
              <li>
                <a
                  href="#home"
                  class="text-gray-300 hover:text-pink-400 transition-colors"
                  >Home</a
                >
              </li>
              <li>
                <a
                  href="#gallery"
                  class="text-gray-300 hover:text-pink-400 transition-colors"
                  >Gallery</a
                >
              </li>
              <li>
                <a
                  href="#services"
                  class="text-gray-300 hover:text-pink-400 transition-colors"
                  >Services</a
                >
              </li>
              <li>
                <a
                  href="#contact"
                  class="text-gray-300 hover:text-pink-400 transition-colors"
                  >Contact</a
                >
              </li>
              <li>
                <a
                  href="tel:<%= business.phone %>"
                  class="text-gray-300 hover:text-pink-400 transition-colors"
                  >Book Now</a
                >
              </li>
            </ul>
          </div>

          <!-- Services -->
          <div>
            <h3 class="text-lg font-semibold mb-6">Popular Services</h3>
            <ul class="space-y-3">
              <% business.services.slice(0, 5).forEach(service => { %>
              <li
                class="text-gray-300 hover:text-pink-400 transition-colors cursor-pointer"
              >
                <%= service.name %>
              </li>
              <% }) %>
            </ul>
          </div>

          <!-- Contact Info -->
          <div>
            <h3 class="text-lg font-semibold mb-6">Contact Info</h3>
            <div class="space-y-4">
              <div class="flex items-center text-gray-300">
                <i class="fas fa-phone text-pink-500 mr-3"></i>
                <a
                  href="tel:<%= business.phone %>"
                  class="text-sm hover:text-pink-400 transition-colors"
                >
                  <%= business.phone %>
                </a>
              </div>
              <div class="flex items-center text-gray-300">
                <i class="fas fa-envelope text-pink-500 mr-3"></i>
                <a
                  href="mailto:<%= business.email %>"
                  class="text-sm hover:text-pink-400 transition-colors"
                >
                  <%= business.email %>
                </a>
              </div>
            </div>

            <!-- Business Hours -->
            <div class="mt-6">
              <h4 class="text-sm font-semibold text-pink-400 mb-2">
                Business Hours
              </h4>
              <p class="text-gray-400 text-xs">
                <%= business.hours.weekdays %>
              </p>
              <p class="text-gray-400 text-xs"><%= business.hours.weekend %></p>
            </div>
          </div>
        </div>

        <!-- Social Media & Bottom Bar -->
        <div class="border-t border-gray-800 pt-8">
          <div class="flex flex-col md:flex-row justify-between items-center">
            <div class="flex space-x-6 mb-4 md:mb-0">
              <a
                href="https://instagram.com/<%= business.socialMedia.instagram.replace('@', '') %>"
                class="text-gray-400 hover:text-pink-400 transition-colors"
              >
                <i class="fab fa-instagram text-xl"></i>
              </a>
              <a
                href="https://www.tiktok.com/<%= business.socialMedia.tiktok.replace('@', '') %>"
                class="text-gray-400 hover:text-pink-400 transition-colors"
              >
                <i class="fab fa-tiktok text-xl"></i>
              </a>
              <a
                href="https://yelp.com"
                class="text-gray-400 hover:text-pink-400 transition-colors"
              >
                <i class="fab fa-yelp text-xl"></i>
              </a>
              <a
                href="https://google.com/maps"
                class="text-gray-400 hover:text-pink-400 transition-colors"
              >
                <i class="fab fa-google text-xl"></i>
              </a>
            </div>

            <div
              class="flex flex-col md:flex-row items-center space-y-2 md:space-y-0 md:space-x-6"
            >
              <div class="flex items-center space-x-4">
                <span class="text-yellow-400 text-sm">★★★★★</span>
                <span class="text-gray-400 text-xs">5.0 • 200+ Reviews</span>
              </div>
              <a
                href="tel:<%= business.phone %>"
                class="bg-pink-600 hover:bg-pink-700 text-white px-6 py-2 rounded-full text-sm font-semibold transition-colors"
              >
                <i class="fas fa-phone mr-2"></i>Call Now
              </a>
            </div>
          </div>

          <div class="mt-8 text-center">
            <p class="text-gray-400 text-sm">
              © <%= new Date().getFullYear() %> <%= business.name %>. All rights
              reserved.
              <span class="text-gray-500">|</span>
              <a href="#" class="hover:text-pink-400 transition-colors"
                >Privacy Policy</a
              >
              <span class="text-gray-500">|</span>
              <a href="#" class="hover:text-pink-400 transition-colors"
                >Terms of Service</a
              >
            </p>
            <p class="text-gray-500 text-xs mt-2">
              Professional nail salon serving Style City and surrounding areas
            </p>
          </div>
        </div>
      </div>
    </footer>

    <!-- Floating Book Now Button (Mobile) -->
    <div class="fixed bottom-6 right-6 z-50 md:hidden">
      <a
        href="tel:<%= business.phone %>"
        class="bg-pink-600 hover:bg-pink-700 text-white p-4 rounded-full shadow-lg hover:shadow-xl transition-all transform hover:scale-110"
      >
        <i class="fas fa-phone text-xl"></i>
      </a>
    </div>

    <!-- Scroll to Top Button -->
    <button
      id="scrollToTop"
      class="fixed bottom-6 left-6 bg-gray-800 hover:bg-gray-700 text-white p-3 rounded-full shadow-lg opacity-0 invisible transition-all duration-300 z-50"
    >
      <i class="fas fa-chevron-up"></i>
    </button>

    <script>
      // Smooth scrolling for navigation links
      document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
        anchor.addEventListener("click", function (e) {
          e.preventDefault();
          const target = document.querySelector(this.getAttribute("href"));
          if (target) {
            target.scrollIntoView({
              behavior: "smooth",
              block: "start",
            });
          }
        });
      });

      // Scroll to top button functionality
      const scrollToTopBtn = document.getElementById("scrollToTop");

      window.addEventListener("scroll", () => {
        if (window.pageYOffset > 300) {
          scrollToTopBtn.classList.remove("opacity-0", "invisible");
        } else {
          scrollToTopBtn.classList.add("opacity-0", "invisible");
        }
      });

      scrollToTopBtn.addEventListener("click", () => {
        window.scrollTo({
          top: 0,
          behavior: "smooth",
        });
      });

      // Form submission handling
      const bookingForm = document.querySelector("form");
      if (bookingForm) {
        bookingForm.addEventListener("submit", function (e) {
          e.preventDefault();
          alert(
            "Thank you for your booking request! We will contact you shortly to confirm your appointment."
          );
        });
      }

      // Mobile menu toggle (if needed)
      const mobileMenuToggle = document.querySelector(".mobile-menu-toggle");
      const mobileMenu = document.querySelector(".mobile-menu");

      if (mobileMenuToggle && mobileMenu) {
        mobileMenuToggle.addEventListener("click", () => {
          mobileMenu.classList.toggle("hidden");
        });
      }
    </script>
  </body>
</html>
